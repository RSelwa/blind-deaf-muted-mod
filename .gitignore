# Gradle / build
.gradle/
build/
*/build/
!gradle/wrapper/gradle-wrapper.jar

# Loom
.loom-cache/
run/
run*/
*/run/
*/run*/

# Docker world data
docker/world/
docker/data/

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath
bin/

# OS
.DS_Store
Thumbs.db

# Logs / runtime
*.log
hs_err_pid*

# Local env / secrets
.env
.env.local

# Vite site
node_modules/
site/dist/

# Keep the distributable mod jar tracked so it's downloadable from the site.
# (build/ above is ignored; this copy under site/public/downloads/ is committed
#  on purpose — re-copy it after each `:mod:build` + version bump.)
!site/public/downloads/*.jar
